### Agent Context Update: Initial Docusaurus Setup

**Technologies added to agent context:**
- Docusaurus (framework)
- React (library)
- Node.js (runtime)
- Markdown/MDX (content format)
- GitHub Pages (deployment for frontend)

**Key concepts:**
- Docusaurus project initialization (`npx create-docusaurus`)
- Sidebar configuration (`sidebars.js`)
- Custom React components in Markdown (for future "Swizzling")
- CSS theming (`custom.css`)
